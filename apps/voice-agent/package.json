{
  "name": "@kortix/voice-agent",
  "version": "0.1.0",
  "description": "LiveKit agent worker \u2014 the spoken voice of a Kortix agent inside a live meeting.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts dev",
    "start": "bun run src/index.ts start",
    "console": "bun run src/index.ts console",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@livekit/agents": "1.5.5",
    "@livekit/rtc-node": "^0.13.31",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.4.0"
  }
}
