/**
 * y0 AI Optimizer Layout
 * Layout for AI optimizer pages
 */

import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'AI Workflow Optimizer | y0 Platform',
  description: 'AI-powered workflow optimization and intelligent automation',
}

export default function AIOptimizerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}