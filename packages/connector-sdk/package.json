{
  "name": "@kortix/connector-sdk",
  "version": "0.9.66",
  "description": "TypeScript SDK for the Kortix Connector — one typed client for every connector (Pipedream / MCP / OpenAPI / Postman / GraphQL / HTTP). Calls run server-side through the Kortix Connector gateway; no third-party secret ever touches your code.",
  "type": "module",
  "license": "Elastic-2.0",
  "homepage": "https://github.com/kortix-ai/suna/tree/main/packages/connector-sdk",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kortix-ai/suna.git",
    "directory": "packages/connector-sdk"
  },
  "keywords": ["kortix", "connector", "connectors", "mcp", "pipedream", "agent", "sdk"],
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["dist", "src", "README.md"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test src/index.test.ts",
    "test:e2e": "bun test src/e2e.test.ts",
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "import": "./dist/index.js"
      }
    },
    "files": ["dist", "README.md"]
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
