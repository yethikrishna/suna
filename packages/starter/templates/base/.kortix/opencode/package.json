{
  "name": "kortix-opencode-config",
  "private": true,
  "//": "Runtime dependencies for the OpenCode config. Provider tools use fetch so cold sessions do not install provider SDK trees.",
  "dependencies": {
    "zod": "4.1.8"
  }
}
