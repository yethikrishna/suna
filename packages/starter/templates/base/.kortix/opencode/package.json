{
  "name": "kortix-opencode-config",
  "private": true,
  "//": "Dependencies for optional Kortix marketplace tools. OpenCode runs `bun install` here at startup so installed tools can import them. The sandbox image pre-warms Bun's cache with these so the boot-time install is offline and fast.",
  "dependencies": {
    "@mendable/firecrawl-js": "^4.25.1",
    "@opencode-ai/plugin": "1.18.19",
    "@tavily/core": "^0.7.3",
    "replicate": "^1.4.0"
  },
  "overrides": {
    "axios": "1.18.0",
    "form-data": "4.0.6"
  }
}
